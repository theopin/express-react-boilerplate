version: "3"
services:
  mongodb:
    container_name: mongo-database
    image: mongo:latest


  backend-service:
    env_file:
      - ./express/.env
    container_name: backend-service
    build: ./express
    depends_on:
      - mongodb
    ports:
      -4000:4000